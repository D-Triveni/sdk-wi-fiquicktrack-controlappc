# Pinned Python packages for .github/workflows/compliance.yml.
#
# sdk-zephyr scripts/ci/check_compliance.py imports reuse, west, etc. at load time
# even when those compliance modules are not selected; keep matching pins here.
# Whitelisted checks additionally need gitlint, pykwalify, vermin, yamllint, junitparser.
#
# Upgrade intentionally when bumping the sdk-zephyr revision used in CI.

pip==25.0.1
setuptools==75.6.0
wheel==0.45.1
PyYAML==6.0.2
gitlint==0.19.1
pykwalify==1.8.0
junitparser==4.0.1
python-dotenv==1.0.1
python-magic==0.4.27
unidiff==0.7.5
reuse==6.2.0
west==1.3.0
yamllint==1.35.1
vermin==1.8.0
jsonschema==4.23.0
tabulate==0.9.0
